aws eks list-clusters --query 'clusters' --output text | tr '\t' '\n' | while read c; do echo "=== $c ==="; aws eks list-access-entries --cluster-name "$c" --query 'accessEntries' --output table 2>/dev/null; done
