aws eks list-clusters --query 'clusters' --output text | tr '\t' '\n' | while read c; do echo "=== $c ==="; aws eks list-fargate-profiles --cluster-name "$c" --query 'fargateProfileNames' --output table 2>/dev/null; done
