aws s3api list-buckets --query 'Buckets[*].Name' --output text | tr '\t' '\n' | while read b; do status=$(aws s3api get-bucket-versioning --bucket "$b" --query 'Status' --output text 2>/dev/null); printf "%-50s %s\n" "$b" "${status:-Disabled}"; done
