if [ -z "$DOMAIN" ]; then
  echo "Usage: DOMAIN=example.com i66 ssl/check"
  exit 1
fi
echo "=== Certificate for $DOMAIN ==="
echo | openssl s_client -connect $DOMAIN:443 -servername $DOMAIN 2>/dev/null \
  | openssl x509 -noout -subject -issuer -dates -fingerprint -serial
