mysql -D "${DB:?DB env var required}" -e "SELECT table_name, ROUND((data_length+index_length)/1024/1024,2) AS size_mb, table_rows FROM information_schema.tables WHERE table_schema='${DB}' ORDER BY size_mb DESC LIMIT 20;"
