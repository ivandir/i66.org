gcloud alpha monitoring channels list --format='table(displayName,type,enabled,name)' 2>/dev/null || \
  gcloud monitoring channels list --format='table(displayName,type,enabled)' 2>/dev/null || \
  echo "No notification channels found"
